LAST_RUN: 2026-04-09T20:25:50-04:00
ITERATION: 72
SPEC_COMPLETED: 073-record-based-property-schemas.md
WHAT_WAS_DONE: Replaced manual ObjectNode building in property builders with immutable records implementing a sealed PropertySchema interface. Jackson serializes records directly to JSON Schema via valueToTree(). Created 9 record types (StringPropertySchema, IntegerPropertySchema, NumberPropertySchema, BooleanPropertySchema, EnumPropertySchema, TitledEnumPropertySchema, MultiSelectPropertySchema, TitledMultiSelectPropertySchema, LegacyEnumPropertySchema) plus supporting records (EnumOption, EnumItemsSchema, TitledEnumItemsSchema). Updated all 7 builders to produce records with no-arg constructors, description()/title()/optional() methods, and build() returning the record. ElicitationSchemaBuilder now stores Map<String, PropertySchema> and derives the required array from each property's required() flag at build time (default: required=true). Removed the explicit required() method from ElicitationSchemaBuilder.
VERIFICATION: mvn verify — BUILD SUCCESS, all 92 tests pass (unit + integration + conformance, spotless, license checks), 0 failures
STATUS: MORE_WORK
NEXT_SPEC: none
NOTES: The @JsonProperty("type") annotation is needed on each record's type() override since it's an interface method, not a record component. The @JsonInclude(NON_NULL) on the sealed interface is inherited by all records. The @JsonIgnore on required() keeps it out of serialized JSON. The old ElicitationSchemaBuilder.required(String...) method was removed — two callers in DefaultMcpStreamContextTest and ConformanceTools were updated (properties now default to required=true so the calls were simply removed). New PropertySchemaSerializationTest covers JSON output verification for all record types.
