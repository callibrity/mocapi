LAST_RUN: 2026-04-18T22:38:32-04:00
ITERATION: 96
SPEC_COMPLETED: 168-mdc-attributes-for-handler-invocations.md
WHAT_WAS_DONE: Added SLF4J MDC correlation keys scoped to every handler invocation. New McpMdcKeys class in mocapi-server/.../observability holds the four key constants (mcp.session, mcp.request, mcp.handler.kind, mcp.handler.name) and the four handler-kind constants (tool, prompt, resource, resource-template). New McpMdcScope (AutoCloseable) in the same package pushes a per-invocation subset into MDC, reading the session id from McpSession.CURRENT when bound, skipping null values, and — crucially — skipping keys that already have a value so it never clobbers MDC state set by an upstream filter; close() removes only the keys the scope itself added. Wired try-with-resources around the dispatch call in McpToolsService.invokeTool (KIND_TOOL + tool name), McpPromptsService.getPrompt (KIND_PROMPT + prompt name), and McpResourcesService.readResource (KIND_RESOURCE + exact URI for direct hits, KIND_RESOURCE_TEMPLATE + uriTemplate string for template matches). Request id is null for this iteration (spec notes this is acceptable since ripcurl doesn't easily surface it). Added McpMdcScopeTest covering every-key-set, close-removes-only-added (pre-existing entry preserved), null requestId skipped, and session-not-bound. Added McpToolsServiceMdcTest integration test that drives service.callTool with a capturing McpTool and asserts MDC is populated during invocation and empty after — including the throwing-tool path. CHANGELOG Unreleased > Added entry and docs/interactive-guide.md MDC section with the virtual-threads / executor-propagation caveat both in place.
VERIFICATION: mvn verify -pl '!mocapi-conformance' — BUILD SUCCESS. Had to remove a stale mocapi-test/target directory left behind by the in-progress mocapi-test module removal (not part of this spec's work) so the reactor wouldn't pick up orphan artifacts; after that verify is clean.
STATUS: MORE_WORK
NEXT_SPEC: 169-structured-validation-error-text.md
NOTES: There is in-progress unstaged work in the tree from outside this session removing the mocapi-test module (parent pom.xml, mocapi-bom/pom.xml, README.md all have unstaged edits; mocapi-test/* files are staged deletes). I did not touch any of that — my changes are only the observability package, CHANGELOG entry, interactive-guide MDC section, and the three handler-service edits plus the two new tests. The next iteration should be aware this state is still pending. Also note: McpMdcScope intentionally declines to overwrite a pre-existing MDC entry and doesn't track it for removal, which preserves upstream filter state (e.g. a servlet filter that sets mcp.session earlier in the chain). The unit test "closeRemovesOnlyKeysItAdded" covers this.
