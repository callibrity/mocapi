LAST_RUN: 2026-04-18T22:35:00-04:00
ITERATION: 95
SPEC_COMPLETED: 167-mcp-logger-interface.md
WHAT_WAS_DONE: Replaced the eight per-level log convenience methods on McpToolContext (debug/info/notice/warning/error/critical/alert/emergency) with a new SLF4J-shaped McpLogger obtained via ctx.logger(name) or ctx.logger() (named after the current handler). Added public McpLogger interface and package-private ContextMcpLogger (final, using SLF4J MessageFormatter for {} substitution) in mocapi-api. Added a default McpToolContext#isEnabled(LoggingLevel) that returns true, plus handlerName() that returns "mcp" by default; ContextMcpLogger calls isEnabled() for per-level gating so the server-side McpSession isn't referenced from mocapi-api. DefaultMcpToolContext overrides isEnabled() (consults McpSession.CURRENT like the existing log() fallback) and handlerName() (new constructor param); McpToolsService#invokeTool now passes the tool name. Added slf4j-api dependency to mocapi-api. New ContextMcpLoggerTest covers all four scenarios from the spec. Updated McpToolContextDefaultMethodsTest, DefaultMcpToolContextTest, and MockMcpToolContextTest to migrate away from the removed convenience methods. Updated docs/interactive-guide.md logging section and added a CHANGELOG [Unreleased] / Breaking changes entry with a before/after migration snippet.
VERIFICATION: mvn verify -pl '!mocapi-conformance' — BUILD SUCCESS. Spotless auto-applied one trailing-newline fix in McpToolContextDefaultMethodsTest.java.
STATUS: MORE_WORK
NEXT_SPEC: 168-mdc-attributes-for-handler-invocations.md
NOTES: Chose to add a default McpToolContext#isEnabled(LoggingLevel) so ContextMcpLogger (in mocapi-api) can gate levels without referencing McpSession (which lives in mocapi-server). DefaultMcpToolContext overrides isEnabled() to consult McpSession.CURRENT exactly as its log() method already does. The existing log() fallback path is unchanged so the "no session = permit everything" behavior matches what the spec asked for. Constructor chain on DefaultMcpToolContext was grown (not replaced) so older callers that didn't pass toolsService/handlerName keep working. The per-level logger on McpLogger uses SLF4J's `warn` (not `warning`) to match SLF4J conventions; the MCP LoggingLevel enum value is still WARNING, so the wire format is unchanged.
