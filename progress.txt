LAST_RUN: 2026-04-10T08:03:00-04:00
ITERATION: 22
SPEC_COMPLETED: 115-mocapi-session-store-jdbc.md
WHAT_WAS_DONE: Created new mocapi-session-store-jdbc module with JdbcMcpSessionStore (all 5 McpSessionStore methods via JdbcTemplate, dialect-specific upsert via JdbcDialect enum for PostgreSQL/MySQL/H2, auto-detection via DatabaseMetaData, configurable table name, periodic cleanup of expired rows via ScheduledExecutorService with configurable interval and disable switch) and JdbcMcpSessionStoreAutoConfiguration (conditional on DataSource + JdbcTemplate, before MocapiAutoConfiguration). Ships three schema SQL files for PostgreSQL, MySQL, and H2.
VERIFICATION: mvn verify — BUILD SUCCESS, full reactor green. Module tests: 13 passed (10 H2 integration tests for JdbcMcpSessionStore including round-trip, all-fields round-trip, TTL expiration, touch extends TTL, TTL preservation on update, delete, find/delete nonexistent, cleanup of expired rows, concurrent upsert + 3 ApplicationContextRunner tests for auto-configuration). Spotless and license checks pass. PostgreSQL Testcontainers IT test (9 tests) runs via failsafe when Docker is available.
STATUS: DONE
NEXT_SPEC: none
NOTES: Duration property for cleanup-interval is parsed as String then Duration.parse() in auto-config because ApplicationContextRunner doesn't register Spring Boot's duration converters. Dialect auto-detection uses DataSource.getConnection().getMetaData().getDatabaseProductName(). JdbcMcpSessionStore implements AutoCloseable to shut down the cleanup executor on context close.
