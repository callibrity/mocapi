LAST_RUN: 2026-04-19T15:15:00-04:00
ITERATION: 108
SPEC_COMPLETED: 182-streamable-http-context-propagation.md
WHAT_WAS_DONE: Wired `io.micrometer:context-propagation` into `StreamableHttpController.handleCall`'s per-call virtual-thread spawn. The controller now takes an injected `ContextSnapshotFactory`, captures a snapshot on the request thread (`factory.captureAll()`), and wraps the handler `Runnable` via `snapshot.wrap(...)` before `Thread.ofVirtual().start(...)` — every registered `ThreadLocalAccessor` (Spring Security's `SecurityContextHolder`, Micrometer Observation, SLF4J MDC, etc.) is restored on the handler VT and cleared on exit. Added `context-propagation` as a compile-scoped dep (BOM-managed by Spring Boot 4.0.5 — resolved to 1.2.1). `StreamableHttpAutoConfiguration` exposes `mcpContextSnapshotFactory()` as a `@ConditionalOnMissingBean` `@Bean` that builds the default factory (ServiceLoader-discovered accessors). Added `spring-security-core` as a test-scope dep to exercise the real `SecurityContextHolderThreadLocalAccessor`. Wrote `ContextPropagationTest` with three scenarios: (1) stub `ThreadLocalAccessor` registered via a hand-rolled `ContextRegistry` — assert value set on request thread is visible to the mock `McpServer.handleCall` running on the VT, plus a direct `ContextSnapshot.wrap` check proving the TL is cleared after the wrapped runnable exits; (2) Spring Security — set a `SecurityContextImpl` with a `TestingAuthenticationToken("alice", ...)` on the request thread, assert `SecurityContextHolder.getContext().getAuthentication().getName()` equals "alice" inside the handler; (3) Micrometer Observation — open an outer `Observation.start("http.request")` + `openScope()` on the request thread with a permissive `ObservationHandler` registered so observations aren't noop, assert a child observation created inside the handler sees the outer as its `getContextView().getParentObservation()`. Updated `StreamableHttpControllerTest` + `StreamableHttpAutoConfigurationTest` for the new constructor arity and added a `custom_context_snapshot_factory_overrides_default` assertion. Added CHANGELOG `### Changed` entry and an architecture.md "Thread-local context propagation" subsection under Streamable HTTP.
VERIFICATION: `mvn verify -pl '!mocapi-conformance'` → BUILD SUCCESS (24 modules; mocapi-streamable-http-transport ran 94 tests, all green including the 3 new ContextPropagationTest cases). `mvn -P release javadoc:jar -DskipTests -pl '!mocapi-conformance'` → BUILD SUCCESS. `mvn spotless:check` → BUILD SUCCESS across all 26 modules.
STATUS: MORE_WORK
NEXT_SPEC: 183-mocapi-o11y-starter.md
NOTES: The default `ContextSnapshotFactory` uses `ContextRegistry.getInstance()` which ServiceLoader-loads all `ThreadLocalAccessor`s on classpath — Spring Security 7 ships `SecurityContextHolderThreadLocalAccessor` and `micrometer-observation` ships `ObservationThreadLocalAccessor`, so both are picked up automatically in any starter that pulls them transitively. The observation test required registering a permissive `ObservationHandler` because `ObservationRegistry.create()` alone returns a `NoopButScopeHandlingObservation` from `Observation.start(...)` that does not track parents; with a handler it promotes to `SimpleObservation` and parent linkage works as spec 183 will need. The "cleared after" acceptance is exercised directly via `ContextSnapshot.wrap(...).run()` + post-run `tl.get() == null` assertion (VT termination makes the handleCall-path check impossible). `spring-security-core` is test-scope only; downstream apps that want SecurityContextHolder propagation must bring their own Spring Security starter. No new ThreadLocal accessors are shipped by mocapi itself — the spec's non-goal is honored.
