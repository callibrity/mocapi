LAST_RUN: 2026-04-19T15:41:00-04:00
ITERATION: 110
SPEC_COMPLETED: 184-simplify-mocapi-o11y.md
WHAT_WAS_DONE: Deleted `McpObservationContext` and `McpObservationConvention` from `mocapi-o11y`. Collapsed `McpObservationInterceptor` to a 3-arg constructor (`registry, handlerKind, handlerName`) deriving `observationName` as `"mcp." + handlerKind`, exposing public `HANDLER_KIND_KEY` / `HANDLER_NAME_KEY` tag-key constants, and calling `Observation.createNotStarted(name, registry)` without a context supplier. `MocapiO11yAutoConfiguration` lost the four `*_OBSERVATION_NAME` constants; each of the four customizer beans now constructs the interceptor with three args. Interceptor test updated to the 3-arg constructor (5 tests, no reference to the deleted types). Autoconfig + meter integration tests unchanged and still green.
VERIFICATION: `mvn verify -pl '!mocapi-conformance'` → BUILD SUCCESS (28 modules, `mocapi-o11y` 5 tests, `mocapi-o11y-spring-boot-starter` 3 tests, all green). `mvn -P release javadoc:jar -DskipTests -pl '!mocapi-conformance'` → BUILD SUCCESS. `mvn spotless:check` → BUILD SUCCESS.
STATUS: MORE_WORK
NEXT_SPEC: 185-mocapi-actuator-starter.md
NOTES: A new spec 185-mocapi-actuator-starter.md appeared in `specs/` during this iteration — will be picked up next run. Observation names (`mcp.tool` / `mcp.prompt` / `mcp.resource` / `mcp.resource_template`) and tag keys (`mcp.handler.kind` / `mcp.handler.name`) are unchanged, so no downstream dashboard or trace consumer is affected. Tag-key constants moved from the deleted `McpObservationConvention` (where they were named `HANDLER_KIND` / `HANDLER_NAME`) to `McpObservationInterceptor` (renamed to `HANDLER_KIND_KEY` / `HANDLER_NAME_KEY` per spec) — these were never shipped in a release, so no breakage for external users.
