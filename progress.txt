LAST_RUN: 2026-04-19T16:17:24-04:00
ITERATION: 112
SPEC_COMPLETED: 186-consolidate-feature-starters-into-autoconfigure.md
WHAT_WAS_DONE: Consolidated the five feature-level Spring Boot starters (mocapi-oauth2-spring-boot-starter, mocapi-jakarta-validation-spring-boot-starter, mocapi-logging-spring-boot-starter, mocapi-o11y-spring-boot-starter, mocapi-actuator-spring-boot-starter) into a single new `mocapi-autoconfigure` module. Created a new `mocapi-jakarta-validation` code module bundling the same runtime deps the old jakarta starter packaged (spring-boot-starter-validation + methodical-jakarta-validation + ripcurl-jakarta-validation). The two transport starters (streamable-http, stdio) remain as user-facing packaging modules that now depend on `mocapi-autoconfigure` + their transport module. Moved every `*AutoConfiguration` class into `mocapi-autoconfigure` preserving package names where sensible — the server autoconfigs (`MocapiServer*AutoConfiguration`, `SubstrateOrderingAutoConfiguration`) kept their `com.callibrity.mocapi.server.autoconfigure` package; the transport autoconfigs collapsed from `.transport.http.autoconfigure` / `.transport.stdio.autoconfigure` to `.transport.http` / `.transport.stdio` (same package as their feature classes); the actuator autoconfig collapsed from `.actuator.autoconfigure` to `.actuator`; o11y collapsed from `.o11y.autoconfigure` to `.o11y`; oauth2 and logging were already in their feature packages and kept them. Added `@ConditionalOnClass` trigger-class guards on every feature autoconfig so classpath absence of the feature module keeps the autoconfig dormant (OAuth2ProtectedResourceMetadataCustomizer for oauth2, McpMdcInterceptor for logging, McpObservationInterceptor for o11y, StreamableHttpController for http transport, StdioTransport for stdio transport; actuator's existing Endpoint.class + @ConditionalOnAvailableEndpoint guards left unchanged). Consolidated the five `AutoConfiguration.imports` files into one at `mocapi-autoconfigure/src/main/resources/META-INF/spring/`. Feature code modules (mocapi-oauth2, mocapi-logging, mocapi-o11y, mocapi-streamable-http-transport, mocapi-stdio-transport) are declared `<optional>true</optional>` compile deps of mocapi-autoconfigure so the autoconfig code compiles against them without dragging them onto consumer classpaths transitively. Added a `test-jar` build execution to `mocapi-server/pom.xml` and a `test-jar` scope=test dep in `mocapi-autoconfigure/pom.xml` so the moved tests can reuse `SubstrateTestSupport` from mocapi-server's test fixtures. Migrated tests: moved `mocapi-server/src/test/.../autoconfigure/*` (except `SessionStoreTestSupport` which stays, used by compliance tests), `CallToolHandlersTest`, `GetPromptHandlersTest` from mocapi-server; moved the full `MocapiOAuth2*Test` suite (8 files) from mocapi-oauth2 keeping `AudienceCheckingOpaqueTokenIntrospectorTest` in place as a pure unit test; moved `MocapiLoggingAutoConfigurationTest` from mocapi-logging (kept `McpMdcInterceptorTest`); moved both o11y tests, all actuator tests, the jakarta integration test + fixtures; moved `StreamableHttpAutoConfigurationTest` and `StdioAutoConfigurationTest` from the transport modules. Fixed `JakartaValidationIntegrationTest.TestApp` and `ActuatorMockMvcTestApp` to `@EnableAutoConfiguration(exclude=…)` the autoconfigs they don't care about — this is necessary inside mocapi-autoconfigure's own tests because every feature module is on the test classpath (via the compile-scope optional deps), so autoconfigs that would be dormant in typical consumer deployments all activate here. Deleted the five feature starter module directories from the working tree and root pom. Rewrote the two transport starter poms as thin packaging-only modules (mocapi-autoconfigure + the transport + any transport-specific deps like spring-boot-starter-webmvc or spring-boot-starter-jackson). Updated root pom `<modules>` list, mocapi-bom to reference the new artifact shape, examples/jakarta-validation/pom.xml to use `mocapi-jakarta-validation`, README.md "Modules" section with a clean per-feature entry listing mocapi-oauth2, mocapi-jakarta-validation, mocapi-logging, mocapi-o11y, and a pointer to `spring-boot-starter-actuator` as the trigger for the actuator endpoint, plus a new `mocapi-autoconfigure` entry. Bulk-updated docs/authorization.md, docs/validation.md, docs/logging.md, docs/observability-roadmap.md to replace every `*-spring-boot-starter` artifactId with its new counterpart. Added a `### Breaking changes` section at the top of CHANGELOG.md `[Unreleased]` laying out the old-to-new artifact mapping as a migration table.
VERIFICATION: `mvn clean install -DskipTests` → BUILD SUCCESS across all 24 modules. `mvn verify -pl '!mocapi-conformance'` → BUILD SUCCESS (mocapi-autoconfigure 140 tests green: MocapiServerAutoConfigurationTest, MocapiServerToolsAutoConfigurationTest, ResourceServiceAutoConfigurationTest, McpToolParamsResolverTest, MocapiRuntimeHintsTest, MocapiServicesAotProcessorTest, CallToolHandlersTest, GetPromptHandlersTest, StreamableHttpAutoConfigurationTest, StdioAutoConfigurationTest, MocapiLoggingAutoConfigurationTest, MocapiO11yAutoConfigurationTest, MocapiO11yMeterIntegrationTest, MocapiActuatorAutoConfigurationTest, McpActuatorEndpointTest, McpActuatorEndpointExposedMockMvcTest, McpActuatorEndpointNotExposedMockMvcTest, JakartaValidationIntegrationTest + 3 nested classes, MocapiOAuth2AutoConfigurationTest, MocapiOAuth2ValidationTest, MocapiOAuth2ResourceResolutionTest, MocapiOAuth2ResourceNameTest, MocapiOAuth2AuthorizationServersTest, MocapiOAuth2ComplianceValidationTest, MocapiOAuth2OpaqueTokenConfigTest, MocapiOAuth2IntegrationTest). `mvn -P release javadoc:jar -DskipTests -pl '!mocapi-conformance'` → BUILD SUCCESS. spotless applied cleanly.
STATUS: MORE_WORK
NEXT_SPEC: none
NOTES: The spec's "MocapiValidationAutoConfiguration" item is not needed as a separate class — mocapi-jakarta-validation just bundles Spring Boot + methodical + ripcurl autoconfigs, and their respective autoconfigure layers already light everything up when the module is on the classpath. Adding a mocapi-owned no-op autoconfig would be empty ceremony. Spec's "mocapi-jakarta-validation code module (extracted from the starter)" was recast as "mocapi-jakarta-validation = same dep bundle the old starter was, minus the -spring-boot-starter artifactId suffix" because the old starter already had no source of its own. The `<optional>true</optional>` feature-module deps in mocapi-autoconfigure cause every feature autoconfig to activate inside this module's own tests (which pull in all feature modules); test code has to explicitly exclude the autoconfigs it doesn't want via `@EnableAutoConfiguration(exclude=…)`. In end-user deployments this is not an issue because users only add the feature modules they want. `mocapi-server`'s `spring-boot-autoconfigure` dependency is now unused (the autoconfigs moved out) but was left in for this iteration to minimize churn; a follow-up can drop it. specs/ queue is empty (only 000-spec-template.md.example remains), so next iteration should either be a no-op or pull from specs/backlog/.
