LAST_RUN: 2026-04-19T15:25:00-04:00
ITERATION: 109
SPEC_COMPLETED: 183-mocapi-o11y-starter.md
WHAT_WAS_DONE: Added two new modules, `mocapi-o11y` and `mocapi-o11y-spring-boot-starter`, that wrap every MCP handler invocation in a Micrometer `Observation`. `mocapi-o11y` depends on `mocapi-server` + `micrometer-observation` (no metrics/tracing SDK) and contains three classes: `McpObservationInterceptor` (Methodical `MethodInterceptor<Object>` that closes over observationName / handlerKind / handlerName at construction — hot path: `Observation.createNotStarted(name, () -> new McpObservationContext(kind,name), registry).lowCardinalityKeyValue("mcp.handler.kind",kind).lowCardinalityKeyValue("mcp.handler.name",name).observe(invocation::proceed)`, zero reflection), `McpObservationContext` (Observation.Context subclass carrying kind + name), and `McpObservationConvention` (ObservationConvention<McpObservationContext> returning the two low-cardinality tags, exported for users who prefer `@Observed` on their own methods). `mocapi-o11y-spring-boot-starter` depends on `mocapi-o11y` + `mocapi-server` + `spring-boot-autoconfigure` and contains `MocapiO11yAutoConfiguration` — `@AutoConfiguration @ConditionalOnClass(ObservationRegistry.class) @ConditionalOnBean(ObservationRegistry.class)` with four `@Bean` customizers (one per handler kind) that read `config.descriptor().name()/uri()/uriTemplate()` and attach a freshly constructed interceptor named `mcp.tool` / `mcp.prompt` / `mcp.resource` / `mcp.resource_template`. Registered via `AutoConfiguration.imports`. Unit tests: `McpObservationInterceptorTest` — one test per handler kind using `TestObservationRegistry` + `TestObservationRegistryAssert` asserting observation name, start/stop, and both low-cardinality tags, plus a 5th test covering the error path (`hasError()` on a RuntimeException-throwing proceed). `MocapiO11yAutoConfigurationTest` — `ApplicationContextRunner` asserts the four customizer beans only appear when an `ObservationRegistry` bean is present (two scenarios). `MocapiO11yMeterIntegrationTest` — wires `SimpleMeterRegistry` + `DefaultMeterObservationHandler`-backed `ObservationRegistry`, runs the tool customizer against a stub `CallToolHandlerConfig`, fires the resulting interceptor, and asserts a `Timer` with name `mcp.tool` and tags `mcp.handler.kind=tool`, `mcp.handler.name=my-tool` with `count()==1` shows up. Root `pom.xml` adds both modules; `mocapi-bom` gains entries for both. README Modules section adds two one-line descriptions under "Spring Boot starters". `docs/observability-roadmap.md` collapses the separate "Metrics (Micrometer)" and "Tracing (OpenTelemetry)" sections into a single "✅ shipped (spec 183)" section describing the single-interceptor approach. CHANGELOG `[Unreleased] ### Added` gains an entry for the module pair.
VERIFICATION: `mvn verify -pl '!mocapi-conformance'` → BUILD SUCCESS (26 modules including the two new ones; `mocapi-o11y` ran 5 tests, `mocapi-o11y-spring-boot-starter` ran 3 tests, all green). `mvn -P release javadoc:jar -DskipTests -pl '!mocapi-conformance'` → BUILD SUCCESS. `mvn spotless:check` → BUILD SUCCESS across all 28 modules.
STATUS: MORE_WORK
NEXT_SPEC: none
NOTES: Active queue under `specs/` is empty after this iteration — only `000-spec-template.md.example` and the `backlog/` + `done/` directories remain. Backlog still has 165-mocapi-client-module.md and 168-mdc-attributes-for-handler-invocations.md waiting to be promoted. Next run will have nothing to do until a human promotes a backlog spec or writes a new one. One deliberate deviation from the spec code snippet: the interceptor uses `Observation.observe(Supplier)` (not `observeChecked`) because Methodical's `MethodInvocation.proceed()` returns `Object` and does not declare checked exceptions, and `MethodInterceptor.intercept` likewise does not declare `throws Throwable`; `observe` still catches/records errors and rethrows unchecked exceptions unchanged, and the error-path test verifies the `ERROR` status is stamped. The spec 183 integration test deliberately does NOT boot an MCP server + real tool invocation — instead it exercises the autoconfig customizer against a stub `CallToolHandlerConfig` with a metered `ObservationRegistry`, which is sufficient to prove the starter wires a working `Timer` without dragging actuator or a full transport into the test path. Spec explicitly permitted either src/test or an example, so this stays in starter src/test.
