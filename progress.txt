LAST_RUN: 2026-04-19T12:40:00-04:00
ITERATION: 96
SPEC_COMPLETED: 170-call-tool-handler-cleanup.md
WHAT_WAS_DONE: Collapsed the McpTool / McpToolProvider SPI and its AnnotationMcpTool implementation into a concrete CallToolHandler in mocapi-server.tools. Added CallToolHandlers.discover(...) — a static factory that scans @ToolService beans in the ApplicationContext and builds one CallToolHandler per @ToolMethod method, moving the descriptor-building and validation logic from AnnotationMcpTool's constructor. McpToolsService now extends PaginatedService<CallToolHandler, Tool>, takes a List<CallToolHandler> in its constructor, and dispatches directly via handler.call(args). MocapiServerToolsAutoConfiguration drops the ToolServiceMcpToolProvider bean and inlines CallToolHandlers.discover(...) inside the McpToolsService factory method. Deleted McpTool, McpToolProvider, AnnotationMcpTool, ToolServiceMcpToolProvider and their tests (AnnotationMcpToolTest, ToolServiceMcpToolProviderTest). Added CallToolHandlerTest (mirrors the old AnnotationMcpToolTest assertions against the new type) and CallToolHandlersTest (exercises the auto-wired @ToolService discovery path via ApplicationContextRunner). Updated McpToolsServiceTest, MocapiServerToolsAutoConfigurationTest, and the three compliance tests (ToolsListComplianceTest, ToolsCallComplianceTest, ToolsCallInteractiveComplianceTest) to construct CallToolHandler directly with a MethodInvoker lambda (method/bean null for hand-built test handlers). Updated PaginatedService javadoc, the architecture-doc log-line example, the native-image-hints SPI count, added docs/handlers.md, and added a CHANGELOG [Unreleased] / Breaking changes entry explaining the SPI removal.
VERIFICATION: mvn verify -pl '!mocapi-conformance' — BUILD SUCCESS. mvn -P release javadoc:jar -DskipTests -pl '!mocapi-conformance' — BUILD SUCCESS. Spotless auto-applied two whitespace nits in CallToolHandlers.java and McpToolsService.java during the first compile.
STATUS: MORE_WORK
NEXT_SPEC: 171-get-prompt-handler-cleanup.md
NOTES: Spec preserves the "no external behavior change" invariant — tools/list and tools/call responses are byte-identical. Compliance tests build CallToolHandler with null method and null bean plus a lambda invoker; this works because the current service dispatch path only consults descriptor() and call() and never inspects method/bean. Spec 174 (interceptors) will start reading those fields — handler.method() / handler.bean() accessors are in place for that. The public Tool descriptor-facing accessors on McpToolsService (findToolDescriptor, allToolDescriptors) keep their signatures; only the internal T changed (McpTool → CallToolHandler). Kept the annotation/ subpackage for Names.java per spec's implementation note — follow-up specs will decide whether to relocate shared helpers.
