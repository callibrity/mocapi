LAST_RUN: 2026-04-19T16:58:30-04:00
ITERATION: 115
SPEC_COMPLETED: 189-extract-mocapi-actuator-module.md
WHAT_WAS_DONE: Extracted a new `mocapi-actuator` code module from `mocapi-autoconfigure`. Created `mocapi-actuator/pom.xml` (depends on `mocapi-server` + `spring-boot-actuator`, following the `mocapi-o11y` pattern). Moved `McpActuatorEndpoint`, `McpActuatorSnapshot`, and `McpActuatorSnapshots` (all three source files, package `com.callibrity.mocapi.actuator`) via `git mv` into `mocapi-actuator/src/main/java`. Moved the pure unit test `McpActuatorEndpointTest` (constructs the endpoint directly and asserts on snapshot shape) alongside the production code into `mocapi-actuator/src/test/java`. Left `MocapiActuatorAutoConfiguration` in `mocapi-autoconfigure` and updated its `@ConditionalOnClass` to include `McpActuatorEndpoint.class` alongside `Endpoint.class` — so the autoconfig stays dormant when `mocapi-actuator` isn't on the classpath. Kept the MockMvc-driven autoconfig tests (`ActuatorMockMvcTestApp`, `McpActuatorEndpointExposedMockMvcTest`, `McpActuatorEndpointNotExposedMockMvcTest`, `MocapiActuatorAutoConfigurationTest`) in `mocapi-autoconfigure` — they exercise the autoconfig end-to-end via `@SpringBootTest`. Added `mocapi-actuator` as `<optional>true</optional>` dependency of `mocapi-autoconfigure` (compile-time reference for the `@ConditionalOnClass` target). Registered the new module in root `pom.xml` (in the feature-code-module block, between `mocapi-o11y` and `mocapi-prompts-mustache`) and in `mocapi-bom/pom.xml` dependencyManagement. Updated the README "Modules" section: dropped the `spring-boot-starter-actuator` bullet that had described the mocapi endpoint as a side effect of adding the starter, replaced it with an explicit `mocapi-actuator` bullet; users now opt in with the mocapi-actuator dep paired with spring-boot-starter-actuator. Updated `docs/observability-roadmap.md` — the "shipped" note now reads "`mocapi-actuator`" (previously "`spring-boot-starter-actuator`"), and explicitly calls out that both artifacts are needed. Updated `CHANGELOG.md`: corrected the spec-186 migration table row for the actuator (was "`spring-boot-starter-actuator` (triggers …)" — not accurate anymore; now "`mocapi-actuator` (pair with `spring-boot-starter-actuator` to activate …)"), and added a new `### Changed` subsection under `[Unreleased]` describing the extraction, the unified feature-module pattern, and the side benefit for users who want snapshot machinery without the autoconfig.
VERIFICATION: `mvn verify -pl '!mocapi-conformance'` → BUILD SUCCESS across all 24 reactor modules including the new `Mocapi - Actuator` module; the relocated `McpActuatorEndpointTest` runs under `mocapi-actuator` surefire and the three autoconfig MockMvc tests continue to run under `mocapi-autoconfigure`. `mvn install -DskipTests -pl '!mocapi-conformance'` → BUILD SUCCESS (needed so the release-profile javadoc build could resolve `mocapi-actuator` as a transitive dep of `mocapi-autoconfigure`). `mvn -P release javadoc:jar -DskipTests -pl '!mocapi-conformance'` → BUILD SUCCESS (doclint green on the new module; every other module's javadoc jar still builds). Spotless + license checks are bound to `validate` and passed as part of `verify`.
STATUS: MORE_WORK
NEXT_SPEC: 190-mocapi-spring-security-guards.md
NOTES: `mocapi-actuator`'s pom uses `spring-boot-actuator` directly (not `spring-boot-starter-actuator`) because the new module only needs the `@Endpoint` / `@ReadOperation` annotations at compile time — not the runtime actuator machinery. Users still need `spring-boot-starter-actuator` on their app classpath for the endpoint to actually serve (that's where `@ConditionalOnAvailableEndpoint` + actuator HTTP mapping come from). The starter is still listed as optional in `mocapi-autoconfigure`'s pom because `MocapiActuatorAutoConfiguration` references `Endpoint.class` and `@ConditionalOnAvailableEndpoint`. Local `~/.m2` install of the new module was required before the release-profile javadoc build could resolve it — a clean CI run will handle this automatically because CI installs each reactor module in order; surfacing it here only in case a future iteration runs `mvn javadoc:jar` without a prior `install`.
