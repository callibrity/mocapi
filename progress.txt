LAST_RUN: 2026-04-07T23:29:21-04:00
ITERATION: 22
SPEC_COMPLETED: 023-mcp-event-id-codec.md
WHAT_WAS_DONE: Implemented McpEventIdCodec in mocapi-core — a stateless, thread-safe utility that produces opaque, tamper-proof, session-bound IDs using AES-256-GCM with HMAC-SHA256 key derivation. Added McpEventIdCodecException for decode failures. Added eventId.masterKey configuration property to MocapiProperties and wired McpEventIdCodec as a conditional bean in MocapiAutoConfiguration (enabled when mocapi.event-id.master-key is set). Comprehensive unit tests cover round-trip encode/decode, wrong-session rejection, tampered ciphertext rejection, nonce uniqueness, malformed input, key validation, and real-world plaintext formats (SSE event IDs, elicitation mailbox keys).
VERIFICATION: mvn verify — BUILD SUCCESS; all tests pass (core + tools + autoconfigure + example); spotless and license checks pass.
STATUS: MORE_WORK
NEXT_SPEC: 024-mcp-elicitation-via-substrate-mailbox.md
NOTES: Controller integration for SSE event ID encryption (SseEventMapper) requires Odyssey to support a subscriber builder with custom event mapping — current OdysseyStream API only exposes subscribe()/resumeAfter() without interception points. The codec is fully implemented and ready for use. Last-Event-ID decryption and elicitation request/response ID encryption will be wired when Odyssey adds mapper support (SSE side) and spec 024 is implemented (elicitation side). The codec bean is conditional on mocapi.event-id.master-key being set, so existing apps are unaffected.
