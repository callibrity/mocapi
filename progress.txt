LAST_RUN: 2026-04-19T17:39:36-04:00
ITERATION: 118
SPEC_COMPLETED: 192-extract-oauth2-logic-from-autoconfig.md
WHAT_WAS_DONE: Extracted all non-annotation logic out of `MocapiOAuth2AutoConfiguration` into five new classes under `com.callibrity.mocapi.oauth2` in the `mocapi-oauth2` module: `MocapiOAuth2Compliance` (static `validate(JwtDecoder, OpaqueTokenIntrospector, List<String>)`), `MocapiOAuth2ResourceResolver` (static `resolve`, `authorizationServers`, `resourceName`), `McpMetadataBuilderCustomizerFactory` (static `create(...)` returning the `Consumer<OAuth2ProtectedResourceMetadata.Builder>`), `McpOAuth2TokenModeConfigurer` (static `apply(rs, jwtDecoder, opaqueTokenIntrospector, audiences)` including the `AudienceCheckingOpaqueTokenIntrospector` wrapping), and `McpOAuth2SecurityFilterChainBuilder` (instance class with constructor capturing all dependencies plus `build(HttpSecurity)` returning the filter chain — matches Spring's `HttpSecurityBuilder` pattern). All extracted methods use concrete types (`List<String>`, `JwtDecoder`, `String springIssuerUri`, `Implementation`) rather than `ObjectProvider`, so they are exercisable from plain unit tests without a Spring context. `MocapiOAuth2AutoConfiguration` now reads top-to-bottom as: annotations, `METADATA_PATH` constant, fail-fast constructor delegating to `MocapiOAuth2Compliance.validate` + `MocapiOAuth2ResourceResolver.resolve`, one `@Bean` method that constructs the builder and calls `build(http)`, and two private `audiencesFrom(...)` / `springIssuerUriFrom(...)` helpers that collapse `ObjectProvider<OAuth2ResourceServerProperties>` to concrete values. Pure-logic tests `MocapiOAuth2ComplianceValidationTest`, `MocapiOAuth2ResourceResolutionTest`, `MocapiOAuth2ResourceNameTest`, `MocapiOAuth2AuthorizationServersTest` were rewritten to call the new classes directly (no `ObjectProvider` stubs, no Spring context) and moved to `mocapi-oauth2/src/test/java/...`. `MocapiOAuth2ValidationTest`, `MocapiOAuth2IntegrationTest`, `MocapiOAuth2AutoConfigurationTest`, and `MocapiOAuth2OpaqueTokenConfigTest` stayed in `mocapi-autoconfigure/src/test` (they exercise the autoconfig via `ApplicationContextRunner` or `@SpringBootTest`). No change to `mocapi-oauth2/pom.xml` (already has all the deps the extracted classes reference) or `mocapi-autoconfigure/pom.xml`. Every fail-fast error message and filter-chain rule preserved verbatim. CSRF-disable Sonar suppression (`java:S4502`) migrated to the builder's `build(HttpSecurity)` method where the disable actually happens.
VERIFICATION: `mvn verify -pl '!mocapi-conformance'` → BUILD SUCCESS across every reactor module (mocapi-oauth2 picked up the new four test classes + four new main classes; mocapi-autoconfigure's full OAuth2 integration + opaque-token + validation-via-context tests all still green). `mvn -P release javadoc:jar -DskipTests -pl '!mocapi-conformance'` → BUILD SUCCESS (doclint clean on every new and edited source file). Spotless was applied once during iteration to bring line-wrapping on new Javadoc/method calls into Google Java Format compliance. Autoconfig final line count: 97 lines (under the 100-line acceptance-criteria target).
STATUS: MORE_WORK
NEXT_SPEC: 193-guards-as-interceptor.md
NOTES: `MocapiOAuth2ResourceResolver.authorizationServers` takes `String springIssuerUri` (nullable/blank meaning "unset") rather than `OAuth2ResourceServerProperties` directly, keeping mocapi-oauth2 free of spring-boot-autoconfigure types at this boundary. The builder takes audiences and springIssuerUri as two separate concrete parameters rather than an `OAuth2ResourceServerProperties` bundle — same rationale. Autoconfig constructor Javadoc and @Bean Javadoc were trimmed to meet the under-100-lines acceptance target; the semantics live in the extracted classes' Javadoc. `MocapiOAuth2ValidationTest` is listed in the spec's "move to mocapi-oauth2" list but drives the autoconfig via `ApplicationContextRunner`, which would create a circular dep (mocapi-autoconfigure depends on mocapi-oauth2, not vice versa); it stayed in mocapi-autoconfigure per the spec's "leave context-driven tests in mocapi-autoconfigure/src/test" caveat. `@SuppressWarnings("java:S4502")` (CSRF-disable justification) moved from the autoconfig @Bean method to `McpOAuth2SecurityFilterChainBuilder.build` — same rule, same comment, just follows the code.
