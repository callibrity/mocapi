LAST_RUN: 2026-04-19T14:03:40-04:00
ITERATION: 104
SPEC_COMPLETED: 178-mocapi-logging-mdc-interceptor.md
WHAT_WAS_DONE: Added two new Maven modules — `mocapi-logging` (code) and `mocapi-logging-spring-boot-starter` (pom-only aggregator) — plus a shared `HandlerKinds` annotation-introspection utility in `mocapi-api`, completing the first observability-starter spec and setting the module-layout pattern for the queued metrics / tracing starters. `mocapi-api` gained `com.callibrity.mocapi.api.handlers.HandlerKinds` with two methods (`kindOf(Method)` → `"tool"` / `"prompt"` / `"resource"` / `"resource_template"` or null, `nameOf(Method)` → annotation's declared name/uri/uriTemplate or null if the method is unannotated) and the four public `KIND_*` string constants. The new `mocapi-logging` module contains three source files — `McpMdcKeys` (string constants `mcp.session`, `mcp.handler.kind`, `mcp.handler.name`, `mcp.request`), `McpMdcInterceptor` (a `MethodInterceptor<Object>` that resolves kind/name via `HandlerKinds`, reads `McpSession.CURRENT.isBound() ? McpSession.CURRENT.get().sessionId() : null` for the session key, stamps exactly the keys whose value is non-null, and in the `finally` branch removes only those keys), and `MocapiLoggingAutoConfiguration` (a plain `@AutoConfiguration` class with `@ConditionalOnClass({MDC.class, MethodInterceptor.class, McpSession.class})` and an `@ConditionalOnMissingBean` `McpMdcInterceptor` `@Bean`) — plus the `META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports` registration. The starter module is pom-only and depends on `mocapi-logging` + `mocapi-streamable-http-spring-boot-starter` per the spec. `mocapi-logging`'s compile deps are `mocapi-api`, `mocapi-server`, `methodical-core`, `slf4j-api`, and `spring-boot-autoconfigure` marked `optional=true` (so the module can be used without Spring). Both modules were added to the parent pom's `<modules>` list and to `mocapi-bom`'s `<dependencyManagement>` block. The unit test `McpMdcInterceptorTest` has ten test cases covering each annotation kind (tool / prompt / resource / resource_template), session-bound vs. unbound (uses `ScopedValue.where(McpSession.CURRENT, session).run(...)` to bind the scoped value), key-removal on normal return vs. thrown exception, preservation of pre-existing MDC entries, suppression of stamping for methods that carry no MCP annotation, and suppression of `mcp.handler.name` when the annotation's `name()` is left at its blank default. All ten tests pass. `McpMdcInterceptor`'s `nameOf` path collapses empty-string `name()` defaults to null before calling `putIfPresent`, so the `@McpTool` (no name) case doesn't write `mcp.handler.name=""`. The spec's `McpSession.id()` example was translated to the actual accessor `McpSession.sessionId()` (verified against `mocapi-server/src/main/java/com/callibrity/mocapi/server/session/McpSession.java`). Documentation: `CHANGELOG.md` `## [Unreleased]` got a new `### Added` section (inserted above `### Changed`) describing the module pair, the MDC key table, and the classpath-on/off contract, plus a second bullet naming the `HandlerKinds` utility; a new `docs/logging.md` page documents the keys, a Logback pattern snippet, the virtual-threads caveat, and how to no-op the interceptor via a `@Bean` override; `docs/observability-roadmap.md` now marks the MDC section "shipped (spec 178)" with a link to `docs/logging.md` and a mention of `HandlerKinds` as the shared tagger for future metrics/tracing starters. No user-visible mocapi API changed — adding the starter to the classpath is the only user action.
VERIFICATION: `mvn verify -pl '!mocapi-conformance'` → BUILD SUCCESS (all modules green; `mocapi-logging` ran 10 tests, 0 failures/errors; streamable-http integration tests + testcontainers-driven redis/postgresql/nats ITs all passed). `mvn -P release javadoc:jar -DskipTests -pl '!mocapi-conformance'` → BUILD SUCCESS (no doclint errors on the new `HandlerKinds` / `McpMdcKeys` / `McpMdcInterceptor` / `MocapiLoggingAutoConfiguration` javadoc). Spotless applied once automatically to break a long javadoc line in `HandlerKinds.java`; post-apply `spotless:check` passed as part of `mvn verify`. Acceptance-criteria spot checks: both modules appear in `pom.xml` `<modules>` and `mocapi-bom/pom.xml` `<dependencyManagement>`; `HandlerKinds` is a public final class at `com.callibrity.mocapi.api.handlers.HandlerKinds` with the expected `kindOf` / `nameOf` / `KIND_*` surface; `McpMdcInterceptor` delegates name/kind extraction to `HandlerKinds` (no duplicated switch); the autoconfig `@ConditionalOnClass` names exactly `MDC.class`, `MethodInterceptor.class`, `McpSession.class`.
STATUS: DONE
NEXT_SPEC: none
NOTES: Spec's `McpSession.id()` code sample was a spec-authoring slip — the real accessor on the `McpSession` record is `sessionId()`. Used the real accessor; no spec-text change made (specs in `done/` are immutable per rules). `mocapi-logging-spring-boot-starter` depends on `mocapi-streamable-http-spring-boot-starter` per the spec's literal instruction — this means a stdio-only consumer who adds the logging starter drags in the streamable-http stack. Worth reconsidering when 179/180 land; could switch to depending on `mocapi-server` only (leave the transport starter choice to the user) so the logging / metrics / tracing starters compose cleanly with either transport. Flagging here rather than deviating unilaterally. `McpMdcInterceptor.nameOf` treats blank (empty-string) annotation defaults as "unset" and skips the key entirely — otherwise every un-named `@McpTool` would log `mcp.handler.name=""`, which is worse than omitting (log-grep patterns expecting `mcp.handler.name=.+` would match empty). `HandlerKinds.nameOf` itself returns the raw empty string to stay honest about what's on the annotation; the blank-to-null filtering is a policy decision of the MDC interceptor. `mcp.request` is declared in `McpMdcKeys` but not yet written — the scoped-value plumbing for the JSON-RPC request id isn't in place. A future spec ("per-invocation scoped state") will wire it; documented the gap in `CHANGELOG.md`, `docs/logging.md`, and `McpMdcKeys` javadoc. No spec has been queued for that follow-up. Active queue is now empty: `specs/` contains only `000-spec-template.md.example` and `backlog/`. Next iteration should stop. If the user wants to continue, promote a spec from `specs/backlog/` — note that `specs/backlog/168-mdc-attributes-for-handler-invocations.md` is the prior draft of this work and should be retired as superseded by 178.
