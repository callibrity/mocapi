LAST_RUN: 2026-04-19T13:38:32-04:00
ITERATION: 102
SPEC_COMPLETED: 176-rename-method-annotations-to-mcp-prefix.md
WHAT_WAS_DONE: Renamed the four method-level handler annotations to drop the `Method` suffix and adopt the `Mcp` domain prefix: `@ToolMethod` -> `@McpTool`, `@PromptMethod` -> `@McpPrompt`, `@ResourceMethod` -> `@McpResource`, `@ResourceTemplateMethod` -> `@McpResourceTemplate`. The four annotation files under `mocapi-api/src/main/java/com/callibrity/mocapi/api/{tools,prompts,resources}/` were git-mv'd and their declared `@interface` names updated to match. A single `sed` pass (ResourceTemplateMethod -> McpResourceTemplate first, so the ResourceMethod substring replacement doesn't cannibalize it) rewrote every reference across 49 files: the four server-side handler discovery helpers (CallToolHandlers, GetPromptHandlers, ReadResourceHandlers, ReadResourceTemplateHandlers), the AOT processor (MocapiServicesAotProcessor) and its test, every test fixture under mocapi-server/src/test/java/**/tools/util and the jakarta-validation starter's fixtures, every example app (example-autoconfigure, stdio, jakarta-validation), the mocapi-conformance tool/prompt/resource fixtures, the McpToolContext javadoc that referenced `@ToolMethod`, the jakarta-validation starter and example pom.xml `<description>` prose, README.md, and every docs/*.md file that quoted these annotations (handlers, validation, tools-guide, prompts-guide, resources-guide, interactive-guide, externalizing-metadata, native-image-hints). CHANGELOG.md's historical entries were excluded from the sed so they keep referring to the old names for the releases where those names were true; a new `[Unreleased]` -> `Breaking changes` entry was added describing the rename and providing the exact four-token sed migration recipe (with the ResourceTemplate-before-Resource ordering note). Class-level `@ToolService` / `@PromptService` / `@ResourceService` annotations were left untouched as the spec requires. Spotless apply reflowed one javadoc paragraph in McpToolContext.java where the shorter replacement token let the prose fit on fewer wrap lines.
VERIFICATION: mvn verify -pl '!mocapi-conformance' -- BUILD SUCCESS (338 unit + integration tests pass). mvn -P release clean package -DskipTests -pl '!mocapi-conformance' -- BUILD SUCCESS (release profile ran the javadoc jar; no doclint errors on any of the renamed annotations or the McpToolContext javadoc update). Acceptance-criteria greps all clean: `@ToolMethod|@PromptMethod|@ResourceMethod|@ResourceTemplateMethod` returns zero java hits, `ToolMethod.class|...` returns zero hits, and the four old `import com.callibrity.mocapi.api.*` lines return zero hits (excluding specs). `@ToolService`/`@PromptService`/`@ResourceService` still present in the Jakarta fixtures (verified).
STATUS: MORE_WORK
NEXT_SPEC: 177-drop-service-annotations-and-centralize-handler-discovery.md
NOTES: Sed ordering was: ResourceTemplateMethod first, then ResourceMethod, then PromptMethod, then ToolMethod -- doing ResourceMethod before ResourceTemplateMethod would have rewritten `ResourceTemplateMethod` to `ResourceTemplateMcpResource` before the ResourceTemplateMethod rule could fire. The same ordering is documented in the CHANGELOG migration recipe for downstream users. Only identifiers that literally *are* the annotation type were renamed -- unrelated internal identifiers that happen to contain "Method" (MethodUtils, MethodInvoker, MethodInterceptor, MethodSchemaGenerator, JakartaMethodValidatorFactory etc.) are untouched because they don't contain the full token. No AutoConfiguration.imports entries referenced the annotation types directly (the AOT hint class registers them via the MocapiServicesAotProcessor which was updated by the sed pass). CHANGELOG.md was excluded from the sed on purpose -- the historical entries describe past releases and must keep their original prose.
