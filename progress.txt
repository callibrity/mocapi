LAST_RUN: 2026-04-19T16:55:00-04:00
ITERATION: 114
SPEC_COMPLETED: 188-remove-blind-interceptor-autowiring.md
WHAT_WAS_DONE: Removed bean-level `@Autowired(required = false) List<MethodInterceptor<? super X>>` parameters from the three handler autoconfigs (`MocapiServerToolsAutoConfiguration`, `MocapiServerPromptsAutoConfiguration`, `MocapiServerResourcesAutoConfiguration`) and dropped the matching interceptors parameter from the four `*Handlers.build(...)` helpers (`CallToolHandlers`, `GetPromptHandlers`, `ReadResourceHandlers`, `ReadResourceTemplateHandlers`) and their `MutableConfig` constructors — each `MutableConfig` now starts with an empty interceptor list that only customizers populate. Kind-specific trailing interceptors (the tool `InputSchemaValidatingInterceptor`) stay in place. Updated the five test files that called the builders directly to drop the now-removed `List.of()` placeholder arg: `ReadResourceHandlerTest` (2 sites), `ReadResourceTemplateHandlerTest` (2 sites), `GetPromptHandlerTest` (2 sites), `CallToolHandlerTest` (2 sites), and `McpToolsServiceTest` (2 sites, including the guards-variant helper). Updated `docs/handlers.md` to stop describing bean-level `MethodInterceptor` autowiring as a supported extension point — the Interceptor chain section now points users at the `*HandlerCustomizer` SPI and the example switches from a `@Component` interceptor to a customizer `@Bean` that wraps the interceptor. Added a `[Unreleased]` / `### Breaking changes` CHANGELOG entry explaining the removal and the migration path (wrap each former `MethodInterceptor` bean in a customizer `@Bean`). Discovered during verify that Jakarta Validation was an in-tree user of the removed blind-wiring path (methodical's `JakartaValidationAutoConfiguration` ships a `JakartaValidationInterceptor` bean that was implicitly picked up by every handler autoconfig's autowired `List<MethodInterceptor<? super X>>`) — the spec's claim that "no in-tree users left" missed this leg. Added a new `MocapiJakartaValidationAutoConfiguration` in `mocapi-autoconfigure` that contributes four customizer beans (tool / prompt / resource / resource-template) each attaching the shared `JakartaValidationInterceptor` bean via `config.interceptor(...)`. Gated with `@ConditionalOnClass(JakartaValidationInterceptor.class)` + `@ConditionalOnBean(JakartaValidationInterceptor.class)` and ordered with `afterName = "org.jwcarman.methodical.autoconfigure.JakartaValidationAutoConfiguration"` so the `@ConditionalOnBean` check fires after methodical has registered its bean. Added the autoconfig to `META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports` and added `methodical-jakarta-validation` as an optional compile-scope dependency of `mocapi-autoconfigure` so the class is resolvable from the new autoconfig.
VERIFICATION: `mvn verify -pl '!mocapi-conformance'` → BUILD SUCCESS (all modules green; the previously failing `JakartaValidationIntegrationTest` — 10 tests across Wiring / Prompts_get / Resources_read / Tools_call nested classes — now passes under the customizer-based wiring). `mvn -P release javadoc:jar -DskipTests -pl '!mocapi-conformance'` → BUILD SUCCESS. Spotless applied cleanly.
STATUS: MORE_WORK
NEXT_SPEC: 189-extract-mocapi-actuator-module.md
NOTES: The spec's migration example showed `return config -> config.interceptor(new MyInterceptor());` — that's also the pattern used in the new `MocapiJakartaValidationAutoConfiguration`, except the interceptor is an existing bean rather than a newly instantiated one. The `@ConditionalOnBean` + `afterName` pairing is essential: `@ConditionalOnBean` at autoconfig-level is evaluated after the referenced autoconfig has run; without `afterName`, my condition fired before methodical had registered its `JakartaValidationInterceptor` bean and the customizers were silently never registered (symptom: Wiring tests passed because the bean existed, but violations didn't throw because the customizer chain was empty). If a future methodical release exposes a `MethodValidatorFactory` bean type (the pom comment on `mocapi-jakarta-validation` implies this is the intended future direction), the customizer-based wiring here can be dropped in favor of methodical wrapping validation at the invoker-factory level. The `mocapi-jakarta-validation` module still ships no Java code of its own — the autoconfig lives in `mocapi-autoconfigure` alongside the other feature customizer-autoconfigs (logging, o11y, actuator), matching the existing architectural pattern.
