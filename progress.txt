LAST_RUN: 2026-04-19T13:33:00-04:00
ITERATION: 101
SPEC_COMPLETED: 175-methodical-0.6-and-interceptor-adoption.md
WHAT_WAS_DONE: Bumped Methodical to 0.6.0 and ripcurl to 2.7.0, migrated every `*Handlers#discover(...)` helper to the new stateless MethodInvokerFactory + Consumer<MethodInvokerConfig> customizer API, and introduced per-kind List<MethodInterceptor<? super T>> autowiring on each of the four handler autoconfigs (tools = JsonNode, prompts = Map<String,String>, resources = Object, resource templates = Map<String,String>). The four helpers now take a List<MethodInterceptor<? super A>> parameter and thread it into the invoker customizer alongside the resolver list. Tool input-schema validation moved out of McpToolsService into a new package-private InputSchemaValidatingInterceptor appended innermost per CallToolHandler -- McpToolsService loses its validateInput, getInputSchema, and inputSchemas ConcurrentHashMap. Jackson3ParameterResolver moved from the Methodical 0.5 factory-level wiring into the per-invoker tool resolver list (order: McpToolContextResolver, McpToolParamsResolver, Jackson3ParameterResolver -- first-match-wins, Jackson last since it's greedy). ComplianceTestSupport.buildDispatcher updated for ripcurl 2.7's new DefaultAnnotationJsonRpcMethodProviderFactory(MAPPER, invokerFactory, resolvers) constructor and the stateless DefaultMethodInvokerFactory(). All other test DefaultMethodInvokerFactory(List.of(...)) call sites dropped the list; tool-side test resolver lists gained the Jackson3 fallback inline. JakartaValidationIntegrationTest's wiring assertion switched from the retired MethodValidatorFactory / JakartaMethodValidatorFactory SPI to Methodical 0.6's JakartaValidationInterceptor bean; the constraint-violation response shapes (-32602 for prompts+resources, isError=true for tools) still pass end-to-end. pom.xml bumped both versions. Docs: CHANGELOG [Unreleased] gained a Changed entry covering the Methodical+ripcurl bump, the new per-kind MethodInterceptor autowiring extension point, the input-schema-validation interceptor move, the Jackson3 resolver relocation, and the Jakarta wiring switch (no end-to-end behavior change). docs/handlers.md gained an "Interceptor chain" section with a worked timing-interceptor example and a callout that output schemas remain descriptive-metadata-only. docs/observability-roadmap.md's opening hedge flipped from future ("Once mocapi's handler layer lands on Methodical 0.6...") to present ("Now that mocapi's handler layer runs on Methodical 0.6..."). Two unit tests in McpToolsServiceTest (call_tool_with_invalid_input_throws_exception, call_tool_with_null_arguments_falls_to_empty_object) were updated to assert CallToolResult.isError=true instead of a JsonRpcException -- the schema-validation interceptor's JsonRpcException is now caught by invokeTool's generic catch(Exception) and surfaces as an isError result, matching the MCP spec's "input validation errors belong in the result body" guidance. Tool-thrown JsonRpcException retains its legacy isError=true behavior (compliance test tool_throwing_json_rpc_exception_returns_is_error_result_not_protocol_error still green).
VERIFICATION: mvn verify -pl '!mocapi-conformance' -- BUILD SUCCESS (338 unit + integration tests pass, including the Jakarta wiring test, all four handler-autoconfig tests, and ToolsCallComplianceTest). mvn -P release clean package -DskipTests -pl '!mocapi-conformance' -- BUILD SUCCESS (release profile runs the javadoc jar; no doclint errors). Spotless auto-formatted three files on first compile (two main-source files nitpicked, one test file).
STATUS: MORE_WORK
NEXT_SPEC: 176-rename-method-annotations-to-mcp-prefix.md
NOTES: The rethrow-JsonRpcException-from-invokeTool experiment was reverted -- it made schema-validation errors bubble up as protocol -32602 but broke the tool_throwing_json_rpc_exception compliance test, which expects a tool's own JsonRpcException to become isError=true. The correct call per the spec's "No functional behavior change beyond the input-schema-validation move" premise is to let invokeTool's generic catch(Exception) swallow both paths -- the unit tests were updated to match. Downstream observability specs (178-mocapi-logging-mdc-interceptor.md and friends) can now ship as plain MethodInterceptor @Bean declarations and pick up autowiring without touching mocapi APIs. The `@Autowired(required = false) List<...>` null-safe wrap is per Spring convention -- Spring hands back null rather than an empty list when no candidates match the generic type. Interceptor ordering: first-added is outermost, so the InputSchemaValidatingInterceptor (added last in CallToolHandlers.build) runs closest to the reflective call and after any ambient observability interceptors contributed by downstream starters.
