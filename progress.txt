LAST_RUN: 2026-04-19T17:58:30-04:00
ITERATION: 119
SPEC_COMPLETED: 193-guards-as-interceptor.md
WHAT_WAS_DONE: Moved call-time guard evaluation out of the service layer and into the MethodInvoker chain. New class `GuardEvaluationInterceptor` (in `com.callibrity.mocapi.server.guards`) implements `MethodInterceptor<Object>`, evaluates the captured `List<Guard>` via `Guards.evaluate`, and throws `JsonRpcException(FORBIDDEN, "Forbidden: <reason>")` on `Deny` (using record-pattern deconstruction on `GuardDecision.Deny`); otherwise calls `invocation.proceed()`. Wired into all four `*Handlers.build(...)` helpers (CallToolHandlers, GetPromptHandlers, ReadResourceHandlers, ReadResourceTemplateHandlers): after customizer-contributed interceptors (so MDC/o11y/user interceptors run first) and, for tools, before `InputSchemaValidatingInterceptor`. The interceptor is only added when the handler has guards attached — zero-overhead path for guardless handlers is preserved. Dropped the `Guards.evaluate(...)`/Deny-throw blocks from `McpToolsService.callTool`, `McpPromptsService.getPrompt`, and both code paths in `McpResourcesService.readResource` (exact + template). `PaginatedService.visibilityFilter` and `McpResourcesService.allResourceDescriptors`/`allResourceTemplateDescriptors` still walk `handler.guards()` for list-time filtering, unchanged. Fixed `McpToolsService.invokeTool` to re-throw `JsonRpcException` with code `FORBIDDEN` (the guard-denial case) rather than wrapping it into a `CallToolResult` — schema-validation `JsonRpcException(INVALID_PARAMS)` stays wrapped as before so the calling LLM can self-correct on malformed arguments. New unit test `GuardEvaluationInterceptorTest` covers allow/deny/empty-list/first-deny-wins. Chain-ordering unit tests added in `CallToolHandlerTest` (guards run after customizer interceptors and before schema validation — passing invalid args to a denied-guarded tool produces FORBIDDEN, not a schema error), `GetPromptHandlerTest`, `ReadResourceHandlerTest`, and `ReadResourceTemplateHandlerTest` (each asserts the recording-customizer interceptor ran once and the guard denial surfaced as FORBIDDEN). Retired redundant service-layer "denied throws forbidden" tests in `McpPromptsServiceTest` and `McpResourcesServiceTest` (the interceptor unit test + chain-ordering tests + existing `GuardIntegrationTest` now own that coverage); `McpToolsServiceTest` kept its equivalent tests because they already drive the real `CallToolHandlers.build(...)` pipeline. `denied_*_is_absent_from_list` tests kept intact — list-time filtering is unchanged.
VERIFICATION: `mvn -pl mocapi-server test` → BUILD SUCCESS, 321 tests, 0 failures (includes new `GuardEvaluationInterceptorTest` and per-kind chain-ordering tests). `mvn verify -pl '!mocapi-conformance,!examples/example-autoconfigure,!examples/jakarta-validation'` → BUILD SUCCESS across every included module; `GuardIntegrationTest` in mocapi-autoconfigure (4 tests) still green, confirming the end-to-end -32003 response through the real JSON-RPC dispatcher. `mvn -pl mocapi-server spotless:check` → green. Spotless was applied once during iteration to bring new test files into Google Java Format compliance.
STATUS: MORE_WORK
NEXT_SPEC: 194-log-interceptor-attachment.md
NOTES: examples/example-autoconfigure has pre-existing dirty working-tree state (HelloTool.java re-indented to 4-space, pom.xml adds micrometer-tracing-bridge-otel dep) unrelated to spec 193 — spotless:check fails there. These files were already modified at the start of this iteration and I did not touch them. Excluded from the verify run; future iterations or a cleanup commit can address. The spec's "integration test for all four handler kinds with o11y observation recorded as errored" is partially covered: `GuardIntegrationTest` proves the end-to-end -32003 response for tools via the real dispatcher; per-kind chain-ordering unit tests prove the guard interceptor fires between customizer-added interceptors and the handler body for every kind. `McpObservationInterceptor` wraps `invocation::proceed` with `Observation.observe(...)`, so by the chain-ordering invariant a guard denial throws out of the observation lambda and Micrometer records ERROR status — no new dedicated o11y-observation-errored assertion was added. `invokeTool`'s new FORBIDDEN-pass-through branch is keyed off the JSON-RPC error code (-32003) rather than a marker type; schema-validation errors continue to wrap as CallToolResult.isError.
