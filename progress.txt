LAST_RUN: 2026-04-19T15:06:20-04:00
ITERATION: 107
SPEC_COMPLETED: 181-mdc-interceptor-customizer-migration.md
WHAT_WAS_DONE: Migrated `mocapi-logging`'s MDC interceptor off the bean-level `MethodInterceptor<? super ...>` autowire path onto the per-handler customizer SPI from spec 180. `McpMdcInterceptor` now takes `(handlerKind, handlerName)` as final fields — no more per-invocation `HandlerKinds` reflection. `MocapiLoggingAutoConfiguration` exposes four customizer @Beans (`CallToolHandlerCustomizer`, `GetPromptHandlerCustomizer`, `ReadResourceHandlerCustomizer`, `ReadResourceTemplateHandlerCustomizer`), each reading `config.descriptor().name()` / `.uri()` / `.uriTemplate()` and attaching a new per-handler `McpMdcInterceptor`. Dropped the `@ConditionalOnMissingBean` `mcpMdcInterceptor` bean entirely. Rewrote `McpMdcInterceptorTest` to exercise the new constructor-driven behavior (kind/name baked in, session from `McpSession.CURRENT`, blank-name → key omitted, exception-safe cleanup, pre-existing MDC preserved). Added `MocapiLoggingAutoConfigurationTest` that stubs each `*HandlerConfig`, runs each customizer, and asserts the resulting interceptor sets the expected kind and name (tool, prompt, resource URI, resource_template URI). Updated `docs/logging.md` (wiring paragraph now describes the four customizer beans; disabling section updated to recommend `spring.autoconfigure.exclude` since the `@ConditionalOnMissingBean` override knob is gone). Updated `docs/observability-roadmap.md` MDC entry to name the customizer wiring. Added a `[Unreleased]` `### Changed` CHANGELOG entry noting the migration, the ripcurl-layer side effect (MDC no longer double-picked-up on JSON-RPC dispatch — stale/null values on ripcurl-internal log lines are gone), and that the hot path is now reflection-free.
VERIFICATION: `mvn verify -pl '!mocapi-conformance'` → BUILD SUCCESS (24 modules green; mocapi-logging ran 13 tests, 0 errors). `mvn -P release javadoc:jar -DskipTests -pl '!mocapi-conformance'` → BUILD SUCCESS (all javadoc jars built clean, no doclint errors). `mvn spotless:check` → BUILD SUCCESS across all 26 modules.
STATUS: MORE_WORK
NEXT_SPEC: 182-mocapi-o11y-starter.md
NOTES: `mocapi-server` autoconfigs still expose the bean-level `@Autowired(required = false) List<MethodInterceptor<? super ...>>` parameters — deliberate per the spec's "no bean-level path removal yet" rule (external users may still ride that path; follow-up spec will remove it once 181 + 182 have shipped). The `HandlerKinds` helper remains in `mocapi-api` for any downstream code that still needs method-level classification outside of a Config-carrying context. The new `MocapiLoggingAutoConfigurationTest` uses hand-rolled `Stub*Config` implementations (not mocks) to keep the test fast and framework-agnostic — these capture the attached interceptor and invoke it with a real `MethodInvocation.of(...)`; they cast the captured interceptor back to the concrete `McpMdcInterceptor` (a plain cast, not an unchecked one — the project bans `@SuppressWarnings` except for the documented deprecation exception).
