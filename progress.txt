LAST_RUN: 2026-04-19T18:14:30-04:00
ITERATION: 121
SPEC_COMPLETED: 195-hashes-helper.md
WHAT_WAS_DONE: Added `com.callibrity.mocapi.server.util.Hashes` with three `sha256Of(...)` overloads (String, byte[], String...). All produce `"sha256:" + <64 lowercase hex>`. Varargs variant concatenates UTF-8 bytes without delimiter and throws NPE on null chunks (with index in the message). Migrated `McpActuatorSnapshots.schemaDigest` to delegate: `schema == null ? null : Hashes.sha256Of(schema.toString());` and dropped the now-unused `java.nio.charset.StandardCharsets`, `java.security.MessageDigest`, `java.security.NoSuchAlgorithmException`, `java.util.HexFormat` imports. Unit tests cover known vectors (`"hello"` and empty string), varargs concatenation equivalence, empty varargs → empty-string hash, null chunk rejection, null array rejection, bytes/String equivalence, and output shape (prefix + 64 lowercase hex). Single private `newDigest()` helper wraps `NoSuchAlgorithmException` as `IllegalStateException`.
VERIFICATION: `mvn -pl mocapi-server,mocapi-actuator verify` => BUILD SUCCESS (spotless + license + tests green). HashesTest 10/10 pass; McpActuatorEndpointTest 3/3 still pass (existing digest output unchanged). Pre-existing `mocapi-example-autoconfigure` spotless violations still block full `mvn verify` -- unchanged from iteration 10 notes, not caused by this spec; my changes pass spotless.
STATUS: MORE_WORK
NEXT_SPEC: 196-mocapi-audit.md
NOTES: The existing `schemaDigest` call site the spec describes lives in `mocapi-actuator`, not `mocapi-server` (spec text said mocapi-server -- harmless mis-location; the helper itself is in mocapi-server as required, mocapi-actuator already depends on mocapi-server so the migration compiled cleanly). Same pre-existing blockers as prior iterations: `mocapi-example-autoconfigure` spotless violations and OAuth2 autoconfig test failures -- neither touched or caused by this spec. Spec 196 will consume `Hashes.sha256Of(...)` for `arguments_hash`.
