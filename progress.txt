LAST_RUN: 2026-04-19T17:10:00-04:00
ITERATION: 116
SPEC_COMPLETED: 190-mocapi-spring-security-guards.md
WHAT_WAS_DONE: Added `mocapi-spring-security-guards` feature module and its autoconfig in `mocapi-autoconfigure`. New code module (package `com.callibrity.mocapi.security.spring`) contains two method-level annotations (`RequiresScope` — AND semantics; `RequiresRole` — OR semantics, accepts bare or `ROLE_`-prefixed values), two `Guard` implementations (`ScopeGuard`, `RoleGuard`) that read `SecurityContextHolder.getContext().getAuthentication()` at check time, and a `SpringSecurityGuards` static helper that reads annotations off a method and applies guards via a generic `BiConsumer<C, Guard>`. Module pom depends on `mocapi-server` and `spring-security-core`. New `MocapiSpringSecurityGuardsAutoConfiguration` in `mocapi-autoconfigure` (package `com.callibrity.mocapi.security.spring.autoconfigure`) is double-gated by `@ConditionalOnClass({ScopeGuard.class, Authentication.class})` and registers four `*HandlerCustomizer` beans (Tool / Prompt / Resource / ResourceTemplate) each a one-liner delegating to `SpringSecurityGuards.attach(config, config.method(), Config::guard)`. Wired through root `pom.xml` (feature-code-module block), `mocapi-bom/pom.xml`, `mocapi-autoconfigure/pom.xml` (as `<optional>true</optional>`, matching every other feature code module), and the `META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports` file. Tests: per-guard unit tests (`ScopeGuardTest`, `RoleGuardTest`) covering null / anonymous / insufficient / sufficient paths plus constructor empty-value rejection and bare-vs-prefixed role equivalence; `SpringSecurityGuardsTest` covering scope-only / role-only / both / neither annotation shapes; `MocapiSpringSecurityGuardsAutoConfigurationTest` confirming all four customizer beans register when both classpath predicates are satisfied, and that neither beans nor failures appear when `ScopeGuard` or `Authentication` is filtered out (uses `FilteredClassLoader`); `SpringSecurityGuardsIntegrationTest` drives the full autoconfig via `JsonRpcDispatcher` with `SecurityContextHolder.setContext(...)` per test — verifying list-time hiding when unauthenticated, list-time visibility with the required scope, call-time `-32003 Forbidden: unauthenticated` / `Forbidden: missing scope(s): admin:write` / success. Docs: README "Modules" section gains a `mocapi-spring-security-guards` bullet; `docs/guards.md` reference-implementation subsection rewritten to drop the stale "spec 188" pointer and show the actual annotation shape; `docs/authorization.md` gains a new "Per-handler authorization" section covering the annotation story end-to-end (scope/role matching rules, list-hiding behavior, `-32003` shape, why guards beat `@PreAuthorize` for list operations). `CHANGELOG.md` `[Unreleased]` / `### Added` section leads with the new module entry (placed above the existing Guard SPI entry).
VERIFICATION: `mvn verify -pl '!mocapi-conformance'` → BUILD SUCCESS across every reactor module, including the new `Mocapi - Spring Security Guards` module (3 unit-test classes, 16 tests) and the two new autoconfig tests in `mocapi-autoconfigure` (`MocapiSpringSecurityGuardsAutoConfigurationTest` — 3 tests; `SpringSecurityGuardsIntegrationTest` — 5 tests). `mvn install -DskipTests -pl '!mocapi-conformance'` → BUILD SUCCESS. `mvn -P release javadoc:jar -DskipTests -pl '!mocapi-conformance'` → BUILD SUCCESS (doclint clean on every new source file). Spotless + license checks ran as part of `validate` and passed; one initial iteration needed `mvn spotless:apply` to bring line-wrapping in javadoc and one assertion chain into conformance with Google Java Format.
STATUS: MORE_WORK
NEXT_SPEC: 191-user-parameter-resolvers-via-customizer.md
NOTES: The two new autoconfig classes both landed under `com.callibrity.mocapi.security.spring.autoconfigure` — parallel to how `com.callibrity.mocapi.oauth2.MocapiOAuth2AutoConfiguration` lives alongside its feature module package name. `MocapiSpringSecurityGuardsAutoConfiguration` is declared with the explicit double gate `@ConditionalOnClass({ScopeGuard.class, Authentication.class})` rather than relying on Maven's <optional>true</optional> transitively keeping Spring Security off unrelated classpaths — cheap defense-in-depth for users who pull `mocapi-spring-security-guards` in without a Spring Security resource-server setup (guards just no-op in that case instead of ClassNotFoundError-ing at startup). The integration test drives via `JsonRpcDispatcher` with a `WebEnvironment.NONE` Spring Boot test (same shape as the pre-existing `GuardIntegrationTest`); the spec mentioned "drive through the Streamable HTTP transport" but exercising the autoconfig via direct dispatch keeps the test hermetic and avoids duplicating the transport coverage already present in `StreamableHttpAutoConfigurationTest`. The stale "(spec 188)" cross-reference in `docs/guards.md` has been removed in favor of a module-name reference only; the module number turned out to be 190 and future-proofs the doc if we renumber again.
