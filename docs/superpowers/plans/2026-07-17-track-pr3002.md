# Track MCP 2026-07-28 PR #3002 — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Conform mocapi to upstream `71e30695` (PR #3002): make request `clientInfo` optional (conformance fix), and emit `io.modelcontextprotocol/serverInfo` on every successful response `_meta` (SHOULD, default-on opt-out).

**Architecture:** `clientInfo` moves from required to optional in `MetaEnvelopeParser`; its consumers become null-safe. serverInfo is injected once, centrally, in `DefaultMcpServer.handleCall` after `dispatcher.dispatch(call)` and before `transport.send` — rebuilding the result via `JsonRpcCall.result(JsonNode)` with `_meta.serverInfo` merged in.

**Tech Stack:** Java 25, JUnit 5 + AssertJ (+ Mockito), ripcurl (`JsonRpcCall`/`JsonRpcResponse`/`JsonRpcResult`), Jackson (`tools.jackson`), Spring Boot config properties, Maven + Spotless.

**Source of truth:** `docs/superpowers/specs/2026-07-17-track-pr3002-clientinfo-serverinfo-design.md`.

## Global Constraints

- No star imports; no new `@SuppressWarnings`; Spotless-clean (`mvn -q spotless:apply` if needed).
- **Do not commit.** Leave changes in the working tree for the maintainer (commit-only-when-asked). Each task ends green under its module tests.
- TDD: write the failing test first, watch it fail, implement, watch it pass.
- Merge semantics for serverInfo: **never clobber** a handler-set `_meta` value.

---

### Task 1: `clientInfo` optional in `MetaEnvelopeParser`

**Files:**
- Modify: `mocapi-server/src/main/java/com/callibrity/mocapi/server/exchange/MetaEnvelopeParser.java` (~line 81 + helpers)
- Test: `mocapi-server/src/test/java/com/callibrity/mocapi/server/exchange/MetaEnvelopeParserTest.java`

**Interfaces:**
- Produces: `McpExchange.clientInfo()` may now return `null` (consumed by Task 2).

- [ ] **Step 1: Failing tests.** Add to the parser test:
  - `envelope_without_clientInfo_parses_successfully`: build a `_meta` with only `protocolVersion` + `clientCapabilities` (valid values); assert `parse(...)` returns an `McpExchange` with `clientInfo() == null` and no exception.
  - `present_but_malformed_clientInfo_still_fails`: `clientInfo` present but missing `version` → expect `JsonRpcException` with code `-32602`.
- [ ] **Step 2: Run, verify they fail** (the first fails because clientInfo is currently required).
  Run: `mvn -q -pl mocapi-server test -Dtest=MetaEnvelopeParserTest`
- [ ] **Step 3: Implement.** Replace the required `clientInfo` read (`requiredObject(meta, CLIENT_INFO, Implementation.class)`) with an **optional** read: if the key is absent → `null`; if present → deserialize and keep the existing `name != null && version != null` validation (still `-32602` when malformed). Leave `protocolVersion` and `clientCapabilities` required. Add an `optionalObject(meta, key, type)` helper mirroring `requiredObject` but returning `null` on absence.
- [ ] **Step 4: Run, verify pass.** Same command → PASS.

---

### Task 2: Null-safe `clientInfo` consumers

**Files:**
- Modify: `mocapi-audit/.../AuditLoggingInterceptor.java`
- Modify: `mocapi-logging/.../McpMdcInterceptor.java`
- Modify: `mocapi-o11y/.../McpObservationFilter.java`
- Tests: the corresponding `*Test` in each module.

**Interfaces:**
- Consumes: `McpExchange.clientInfo()` (nullable, from Task 1).

- [ ] **Step 1: Failing tests.** In each module's test, add a case where the bound `McpExchange` has `clientInfo() == null`; assert the interceptor/filter runs without NPE and simply omits the clientInfo-derived field(s) (MDC key absent / audit field absent / observation tag absent).
- [ ] **Step 2: Run, verify fail** (NPE or missing guard).
  Run: `mvn -q -pl mocapi-audit,mocapi-logging,mocapi-o11y test`
- [ ] **Step 3: Implement.** In each consumer, guard every `exchange.clientInfo()` dereference with a null check; only emit the clientInfo name/version/title fields when non-null.
- [ ] **Step 4: Run, verify pass.**

---

### Task 3: serverInfo injection seam + constant + property

**Files:**
- Modify: `mocapi-model/.../McpMetaKeys.java` (add `SERVER_INFO`)
- Modify: `mocapi-autoconfigure/.../server/autoconfigure/MocapiServerProperties.java` (add `emitServerInfo`, default `true`)
- Modify: `mocapi-server/src/main/java/com/callibrity/mocapi/server/DefaultMcpServer.java` (constructor + injection)
- Modify: `mocapi-autoconfigure/.../server/autoconfigure/MocapiServerAutoConfiguration.java` (wire the two new constructor args into the `DefaultMcpServer` bean)
- Test: `mocapi-server/src/test/java/com/callibrity/mocapi/server/DefaultMcpServerTest.java`

**Interfaces:**
- Consumes: the existing `Implementation serverInfo` bean (`MocapiServerAutoConfiguration:91`).
- Produces: `McpMetaKeys.SERVER_INFO = "io.modelcontextprotocol/serverInfo"`.

- [ ] **Step 1: Confirm the ripcurl result accessor.** Run
  `javap -cp $(find ~/.m2 -name 'ripcurl-core-*.jar'|grep -v sources|head -1) com.callibrity.ripcurl.core.JsonRpcResult`
  to confirm how the result `JsonNode` is read (expected: a `result()` accessor) and that a successful `dispatch` returns a `JsonRpcResult`. Use the real accessor name in Step 4.
- [ ] **Step 2: Add the constant + property.**
  - `McpMetaKeys.SERVER_INFO = "io.modelcontextprotocol/serverInfo"` (with Javadoc matching the file's style).
  - `MocapiServerProperties`: add `emitServerInfo` (default `true`) with accessor, mirroring existing property style.
- [ ] **Step 3: Failing tests** in `DefaultMcpServerTest` (build a stub `JsonRpcDispatcher` returning `call.result(objectMapper.createObjectNode())` for success, and a `JsonRpcError` for the error case; a capturing `McpTransport`; a fixed `Implementation("srv","Srv","1.2.3",null)`):
  - `successful_result_carries_serverInfo_in_meta`: the sent response's result node has `_meta."io.modelcontextprotocol/serverInfo"` equal to the serialized `Implementation`.
  - `error_response_has_no_serverInfo`: dispatch returns a `JsonRpcError` → sent unchanged, no `_meta` injected.
  - `emit_disabled_skips_injection`: construct with `emitServerInfo=false` → no `_meta.serverInfo`.
  - `existing_result_meta_is_preserved`: dispatcher returns a result whose node already has `_meta` with another key → serverInfo added alongside; the pre-existing key/value remain.
- [ ] **Step 4: Run, verify fail.**
  Run: `mvn -q -pl mocapi-server test -Dtest=DefaultMcpServerTest`
- [ ] **Step 5: Implement.**
  - Extend `DefaultMcpServer`'s constructor with `Implementation serverInfo` and `boolean emitServerInfo`; store them.
  - After `dispatcher.dispatch(call)` returns a non-null `response`, if `emitServerInfo` **and** `response instanceof JsonRpcResult result`: read the result `JsonNode`; if it is an `ObjectNode`, get-or-create its `_meta` `ObjectNode`; if `_meta` lacks `SERVER_INFO`, `set(SERVER_INFO, objectMapper.valueToTree(serverInfo))`; rebuild via `call.result(mutatedResultNode)` and send that. Otherwise send `response` unchanged. Do **not** touch `JsonRpcError` responses.
  - Wire the two new args into the `DefaultMcpServer` bean in `MocapiServerAutoConfiguration` (serverInfo bean + `props.emitServerInfo()`).
- [ ] **Step 6: Run, verify pass.** Then `mvn -q -pl mocapi-autoconfigure test` to confirm the bean wiring compiles/holds.

---

### Task 4: Re-pin schema snapshot + diff note

**Files:**
- Overwrite: `docs/plans/2026-07-28-schema.ts`, `docs/plans/2026-07-28-schema.json`
- Modify: `docs/plans/2026-07-28-schema-diff.md`

- [ ] **Step 1: Re-pull the snapshot** with `curl -sSL` from
  `https://raw.githubusercontent.com/modelcontextprotocol/modelcontextprotocol/main/schema/draft/schema.ts` (and `.json`) into the two files.
- [ ] **Step 2: Diff old vs new** (`git diff`); confirm the only semantic deltas are PR #3002's two changes (clientInfo optional; serverInfo in ResultMetaObject). If other semantic changes appear, STOP and report.
- [ ] **Step 3: Add a dated "2026-07-17 re-diff #2" addendum** to `2026-07-28-schema-diff.md` recording both PR #3002 changes and the new pinned SHA `71e30695`.

---

### Task 5: ADR-0026 + design/constitution/process docs

**Files:**
- Create: `docs/adr/0026-<short-title>.md`
- Modify: `docs/adr/README.md` (index), `docs/constitution.md` (link from I3), a design doc describing the `_meta` envelope (`docs/design/architecture-overview.md` or `transports.md`), `docs/superpowers/README.md` (spec-vs-ADR note)

- [ ] **Step 1: Write ADR-0026** using `docs/adr/_template.md`, status Accepted, today's date. Scope: the **response-`_meta` injection seam** as the canonical place to add server-side `_meta` keys, and the decision to adhere to the `serverInfo` SHOULD by default with an opt-out (`mocapi.emit-server-info`). Note `clientInfo`-optional as spec-tracking. `**Code anchors:**` → `DefaultMcpServer.java`, `McpMetaKeys.java`.
- [ ] **Step 2: Index it** in `docs/adr/README.md`.
- [ ] **Step 3: Link from constitution I3** in `docs/constitution.md` (add ADR-0026 to I3's citations).
- [ ] **Step 4: Update the design doc** describing the envelope to state the new required set (`protocolVersion` + `clientCapabilities`; `clientInfo` optional) and the serverInfo response emission.
- [ ] **Step 5: Add a "Spec vs ADR — when each" note** to `docs/superpowers/README.md`: a spec is a build document (goes stale); an ADR is the durable, citable, supersedable *why* (new/changed invariant, contract change, or a pattern others must follow). Only specs that hit that bar produce an ADR.
- [ ] **Step 6: Link sweep** — confirm the new relative links resolve.

---

### Task 6: Full verification

- [ ] **Step 1:** Run `mvn -q -T1C clean verify` from the repo root. Iterate until BUILD SUCCESS across all modules. Do not weaken assertions to pass.
- [ ] **Step 2:** Leave all changes in the working tree, uncommitted, for the maintainer.

## Self-Review

- **Coverage:** spec Part 1 → Tasks 1–2; Part 2 → Task 3; snapshot → Task 4; ADR/docs → Task 5; verification → Task 6.
- **Placeholders:** `0026-<short-title>` and the "confirm ripcurl accessor" step are deliberate (resolved during implementation), not gaps.
- **Consistency:** `McpMetaKeys.SERVER_INFO`, `emitServerInfo`/`mocapi.emit-server-info`, and the `DefaultMcpServer` constructor additions are referenced identically across Tasks 3 and 5.
