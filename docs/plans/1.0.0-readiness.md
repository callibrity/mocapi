# mocapi 1.0.0 readiness — tracked checklist

Source: the 2026-07-28 five-dimension readiness sweep. Cut 1.0.0 on
**Spring Boot 4.0.5** (defer 4.1.0 / spotless 3.9 / surefire+failsafe M1
to 1.1). Version bump to 1.0.0 is the release trigger, done last.

## Done (on `release/1.0.0-prep`)

- [x] **B1 — Remove `DRAFT-2026-v1` alias** (ADR-0027). Code + tests + docs;
  conformance switched to `--spec-version 2026-07-28`.
- [x] **B2 — Regenerate conformance baseline** (@0.2.0-alpha.10, `--suite all
  --spec-version 2026-07-28`): 79 passed / 13 failed, all 13 self-documented in
  `conformance-expected-failures.yaml`; stale `non-tool-request` waiver dropped.
  Added `test_logging_tool` + `test_streaming_elicitation`.
- [x] **B3 — Reconcile transport-layer ADRs** (0002/0005 superseded; 0003/0004/
  0006 amended; `mocapi-otel` added to 0017; anchors fixed; 0020/0021/0022 anchors).
- [x] **Native-image hint gap** — register `RequestStatePayload` in
  `MocapiRuntimeHints` (+ test + design-doc). MRTR replay now survives native.
- [x] **ADR-0028** — decline SEP-2243 `x-mcp-header` on principle (transport-agnostic).
- [x] Green plugin bumps (jacoco/license/dependency/enforcer).

## Remaining before tagging

### 1. User-facing staleness cleanup (ships to users)
- [ ] `docs/design/authorization-model.md:141` "Initialize bypasses guards"
  → `server/discover` bypasses guards.
- [ ] Delete dead `mocapi.session-encryption-master-key` path:
  `EphemeralMasterKeyEnvironmentPostProcessor` + spring.factories + example
  `README:69-71` + `ConformanceApplication:27-35` + ~11 test property sources.
- [ ] `MocapiO11yAutoConfiguration.java:36-47` javadoc (McpSession#CURRENT /
  mcp.session.id / initialize).
- [ ] `README.md:9` draft spec link → dated; `docs/guides/validation.md:137,65`
  (`InitializeRequest`, 2025-11-25 link); `docs/design/extension-spi.md:126`
  (`McpSession` → `McpExchange`).
- [ ] CHANGELOG: fold in serverInfo, `DiscoverResult.serverInfo` drop,
  `SubscriptionsListen` rename, optional clientInfo, ADR-0026/0027/0028; rename
  `[Unreleased]` → `[1.0.0]`.

### 2. SPI freeze
- [ ] `McpTransport` SPI — add javadoc; decide keep-vs-wrap the leaked ripcurl
  `JsonRpcMessage` before freezing at 1.0.

### 3. Decisions to ratify (owner)
- [ ] Deprecated API policy → recommend **keep** (SEP-2577 12-month compat).
- [ ] Two deferred auth SHOULDs (`scope` on 401, 403 step-up) — implement or defer.
- [ ] Partial cancellation (handler not interrupted on disconnect) — confirm stance.
- [ ] (surfaced by conformance) Explicit tool `inputSchema` support? — defer past 1.0.
- [ ] (surfaced by conformance) Handler API to require an arbitrary client
  capability → `-32021`? — defer past 1.0.

### 4. Release gate → tag
- [ ] `mvn -P release javadoc:jar -DskipTests` (doclint on deprecated surface).
- [ ] Full `mvn verify`.
- [ ] **B4 — Version bump** 0.18.0-SNAPSHOT → 1.0.0 across 25 poms +
  `README.md:49,61`. **(release trigger — last)**

## Post-1.0 (nice-to-have, not blockers)
- Native CI harness (native profile + GraalVM job + elicitation round-trip) — scoped.
- The two conformance-surfaced API questions above, if a user asks.
