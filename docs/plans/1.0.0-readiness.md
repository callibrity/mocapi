# mocapi 1.0.0 readiness — tracked checklist

Source: the 2026-07-28 five-dimension readiness sweep. Cut 1.0.0 on
**Spring Boot 4.0.5** (defer 4.1.0 / spotless 3.9 / surefire+failsafe M1
to 1.1). Version bump to 1.0.0 is the release trigger, done last.

## Blockers (must clear before tagging)

- [ ] **B1 — Remove `DRAFT-2026-v1` alias** (ADR-0027). Code: `McpServer`,
  `MetaEnvelopeParser`, `DiscoverHandler`; tests: `MetaEnvelopeParserTest`,
  `DiscoverHandlerTest`, `DiscoverComplianceTest`, `MocapiStartupBannerTest`;
  docs: `CHANGELOG`, `mocapi-conformance/README`, root `README`. Switch
  conformance to `--spec-version 2026-07-28`.
- [ ] **B2 — Regenerate conformance baseline** against
  `--spec-version 2026-07-28` (tool `@0.2.0-alpha.10`). Remove the stale
  `input-required-result-non-tool-request` waiver; reconcile README counts.
- [ ] **B3 — Reconcile transport-layer ADRs.** ADR-0002 (dead
  `McpServer`↔`McpTransport` contract, dangling `McpEvent`) → Superseded by
  0020; ADR-0005 (deleted SSE-event-ID subsystem) → Superseded by 0020;
  amend ADR-0003/0004/0006 (session/Odyssey text); add `mocapi-otel` to
  ADR-0017; fix ADR-0024 (`DefaultMcpElicitor`) + ADR-0013 (module path)
  anchors; add `Code anchors:` to 0020/0021/0022.
- [ ] **B4 — Version bump** 0.18.0-SNAPSHOT → 1.0.0 across 25 poms +
  `README.md:49,61`. **(release trigger — last)**

## User-facing staleness cleanup (should-fix)

- [ ] `docs/design/authorization-model.md:141` "Initialize bypasses guards"
  → `server/discover` bypasses guards.
- [ ] Delete dead `mocapi.session-encryption-master-key` path:
  `EphemeralMasterKeyEnvironmentPostProcessor` + spring.factories + example
  `README:69-71` + `ConformanceApplication:27-35` + ~11 test property sources.
- [ ] `MocapiO11yAutoConfiguration.java:36-47` javadoc (McpSession#CURRENT /
  mcp.session.id / initialize).
- [ ] `README.md:265,9` RC/draft framing + draft spec links → final.
- [ ] `docs/guides/validation.md:137,65` (`InitializeRequest`, 2025-11-25 link).
- [ ] `docs/design/extension-spi.md:126` (`McpSession` → `McpExchange`).
- [ ] CHANGELOG: fold in serverInfo, `DiscoverResult.serverInfo` drop,
  `SubscriptionsListen` rename, optional clientInfo, ADR-0026; rename
  `[Unreleased]` → `[1.0.0]`.

## Decisions to ratify (owner)

- [ ] Deprecated API policy → recommend **keep** (SEP-2577 12-month
  backward-compat; `LegacyTitledEnumSchema` stays regardless).
- [ ] `McpTransport` SPI — add javadoc; decide keep-vs-wrap the leaked
  ripcurl `JsonRpcMessage` before freezing at 1.0.
- [ ] Two deferred auth SHOULDs (`scope` on 401, 403 step-up) — implement or
  ratify as deferred.
- [ ] Partial cancellation (handler not interrupted on disconnect) — confirm
  1.0 stance.

## Release process

- [ ] `mvn -P release javadoc:jar -DskipTests` (doclint on deprecated surface).
- [ ] Full `mvn verify`.
- [ ] Green plugin bumps (jacoco/license/dependency/enforcer) — applied, folded
  into `release/1.0.0-prep`.
